Config files for Emacs, etc.

To get started on a new server:

```
git clone https://pdg137@github.com/pdg137/config.git
cd config && make
```

Running emacs should automatically install all packages from Melpa.
